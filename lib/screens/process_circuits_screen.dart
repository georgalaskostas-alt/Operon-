import 'package:flutter/material.dart';import '../theme/operon_theme.dart';
class ProcessCircuitsScreen extends StatelessWidget{const ProcessCircuitsScreen({super.key});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Process circuits')),body:ListView(padding:const EdgeInsets.all(16),children:const[Card(child:ListTile(leading:Icon(Icons.account_tree,color:OperonTheme.teal),title:Text('Circuit library'),subtitle:Text('Create unit-specific circuits and link equipment, valves, notes, documents and approved P&ID references.'))),Card(child:ListTile(leading:Icon(Icons.security,color:OperonTheme.teal),title:Text('Controlled knowledge'),subtitle:Text('No operating limit or valve position is invented by OPERON. Values must come from operator/site-approved data.')))]));}
