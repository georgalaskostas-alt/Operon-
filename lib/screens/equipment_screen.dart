import 'package:flutter/material.dart';import '../data/app_store.dart';import '../models/models.dart';import '../theme/operon_theme.dart';import '../widgets/common.dart';import 'equipment_knowledge_screen.dart';
class EquipmentScreen extends StatefulWidget{final AppStore store;const EquipmentScreen({super.key,required this.store});@override State<EquipmentScreen> createState()=>_S();}
class _S extends State<EquipmentScreen>{String q='';@override Widget build(BuildContext c){final items=widget.store.equipment.where((e)=>'${e.tag} ${e.name} ${e.area}'.toLowerCase().contains(q.toLowerCase())).toList();return ListView(children:[const OperonHeader('Equipment',subtitle:'Status, notes and history'),Padding(padding:const EdgeInsets.symmetric(horizontal:20),child:TextField(onChanged:(v)=>setState(()=>q=v),decoration:const InputDecoration(prefixIcon:Icon(Icons.search),hintText:'Search tag or equipment'))),const SizedBox(height:12),...items.map((e)=>Card(margin:const EdgeInsets.symmetric(horizontal:20,vertical:6),child:ListTile(onTap:()=>_details(e),title:Text(e.tag,style:const TextStyle(fontWeight:FontWeight.w800)),subtitle:Text('${e.name}\n${e.note.isEmpty?e.area:e.note}'),isThreeLine:true,trailing:Text(e.state.name,style:const TextStyle(color:OperonTheme.teal,fontSize:11,fontWeight:FontWeight.w700)))),const SizedBox(height:100)]);}
void _details(Equipment e){final history=widget.store.logs.where((x)=>x.equipmentTag==e.tag).toList();showModalBottomSheet(context:context,isScrollControlled:true,showDragHandle:true,builder:(c)=>DraggableScrollableSheet(expand:false,initialChildSize:.72,maxChildSize:.94,builder:(_,scroll)=>ListView(controller:scroll,padding:const EdgeInsets.fromLTRB(20,0,20,40),children:[Text(e.tag,style:const TextStyle(fontSize:28,fontWeight:FontWeight.w800)),Text('${e.name} · ${e.area}',style:const TextStyle(color:OperonTheme.muted)),const SizedBox(height:18),Wrap(spacing:8,runSpacing:8,children:EquipmentState.values.map((s)=>ChoiceChip(label:Text(s.name),selected:e.state==s,onSelected:(_){widget.store.setEquipmentState(e,s);Navigator.pop(c);})).toList()),const SizedBox(height:18),Text(e.note.isEmpty?'No active note':e.note),const SizedBox(height:12),OutlinedButton.icon(onPressed:(){Navigator.pop(c);Navigator.push(context,MaterialPageRoute(builder:(_)=>EquipmentKnowledgeScreen(store:widget.store,tag:e.tag)));},icon:const Icon(Icons.menu_book),label:const Text('Operational knowledge')),const SectionLabel('History'),if(history.isEmpty)const ListTile(title:Text('No recorded events')),...history.map((x)=>ListTile(leading:Text(hhmm(x.createdAt),style:const TextStyle(color:OperonTheme.muted)),title:Text(x.text),subtitle:Text(x.source)))])));}}
